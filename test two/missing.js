var sumDuplicates = function (arr) {
  var dupes = {}

  // iterate through arr
  for (var i = 0; i < arr.length; i++) {
    if (dupes[arr[i]] )
      
    }
  }
  // empty arr
  return 'invalid arr'
}

var test = [1,2,3,5]
console.log(missing(test))
